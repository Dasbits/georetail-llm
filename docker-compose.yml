services:
  postgres:
    image: postgis/postgis:15-3.3
    environment:
      POSTGRES_DB: georetail
      POSTGRES_USER: georetail
      POSTGRES_PASSWORD: georetail123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data: